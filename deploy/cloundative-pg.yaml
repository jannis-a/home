apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cloudnative-pg
  namespace: flux-system
spec:
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./apps/cloudnative-pg
  interval: 10m
  prune: true
  dependsOn:
  - name: cilium
  # TODO: remove
  suspend: true
