apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cloudnative-pg
spec:
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./deploy/cloudnative-pg
  interval: 10m
  prune: true
  dependsOn:
  - name: cilium
  # TODO: remove
  suspend: true
