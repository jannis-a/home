apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: local-static-provisioner
spec:
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./deploy/core/local-static-provisioner
  interval: 10m
  prune: true
