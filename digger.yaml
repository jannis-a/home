generate_projects:
  blocks:
  - block_name: dev
    terragrunt: true
    root_dir: infra/live
    workflow: default
workflows:
  default:
    plan:
      steps:
      - init
      - plan
      - run: echo "Terragrunt generation!"
